services:
  mongodb:
    image: mongodb:v1
    container_name: mongodb

networks:
  default:
    driver: bridge
    name: roboshop
    external: true #true if u have already created the network and want to use it
