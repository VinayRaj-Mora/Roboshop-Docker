services:
  mongodb:
    image: mongodb:v1
    container_name: mongodb
  
  catalogue:
    image: catalogue:v1
    container_name: catalogue
    depends_on:
      - mongodb

networks:
  default:
    name: roboshop
    external: false #true if have created network roboshop in docker
    